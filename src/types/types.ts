export type VotingType = {
    id: string;
    slug: string;
    name: string;
    facultyId?: string;
    img?: string;
    color: string;
} [];

export type CandidateType = {
    id: string;
    slug: string;
    positions: string;
    candidates: string;
} [];

